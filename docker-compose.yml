services:
  nextcloud:
    image: nextcloud:latest
    container_name: nextcloud
    restart: always
    ports:
      - "8081:80"
    volumes:
      - nextcloud_data:/var/www/html
    networks:
      - app-network

  collabora_online:
    build:
      context: .
      dockerfile: ./src/view/Dockerfile
    image: collabora:latest
    container_name: collabora_online
    restart: always
    ports:
      - "9980:9980"
    cap_add:
      - SYS_ADMIN
    environment:
      - extra_params=--o:ssl.enable=false --o:ssl.termination=false --o:storage.wopi.host=192.168.5.11 --o:server_name=192.168.5.11:9980 --o:storage.wopi.host_allow_list=nextcloud,192.168.5.11
    networks:
      - app-network
    depends_on:
      - nextcloud
      - skopeo_controller

  skopeo_controller:
    build:
      context: .
      dockerfile: Dockerfile
    image: skopeo-controller:latest
    container_name: skopeo_controller
    restart: always
    ports:
      - "9000:9000"
    networks:
      - app-network

volumes:
  nextcloud_data:

networks:
  app-network:
    driver: bridge
