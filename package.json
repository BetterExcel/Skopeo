{
  "name": "skopeo-monorepo",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "services/*",
    "shared/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "format": "npm run -ws format",
    "lint": "npm run -ws lint"
  }
}
